import { PrixDinarPipe } from './prix-dinar.pipe';

describe('PrixDinarPipe', () => {
  it('create an instance', () => {
    const pipe = new PrixDinarPipe();
    expect(pipe).toBeTruthy();
  });
});
